[phases.setup]
nixPkgs = ["jdk17", "gradle"]

[phases.build]
cmds = ["cd server && chmod +x gradlew && ./gradlew clean build jar -x test"]

[start]
cmd = "java -jar server/build/libs/server-1.0-SNAPSHOT.jar"

