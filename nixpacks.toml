[phases.setup]
nixPkgs = ["nodejs-18_x", "jdk17", "gradle"]

[phases.build]
cmds = ["cd client && npm install && npm run build && cd ../server && chmod +x gradlew && ./gradlew clean build jar -x test"]

[start]
cmd = "java -jar server/build/libs/server-1.0-SNAPSHOT.jar"

